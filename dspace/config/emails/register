## E-mail sent to DSpace users when they register for an account
##
## Parameters: {0} is expanded to a special registration URL
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')} Account Registration")
#set($phone = ${config.get('mail.message.helpdesk.telephone')})
Um die Registrierung für Ihren SynSICRIS-Account abzuschließen, klicken Sie bitte auf den folgenden Link:

  ${params[0]}

Wenn Sie Hilfe mit Ihren Account benötigen, senden Sie bitte eine Mail an
${config.get("alert.recipient")}.

Ihr SynSICRIS-Team
